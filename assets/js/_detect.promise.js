/**
 * PROMISES FEATURE DETECTION
 */
if(typeof Promise !== "undefined" && Promise.toString().indexOf("[native code]") !== -1) {
    Your browser is unsuspecting, paste your polyfill or library here.
}
